@using Microsoft.AspNetCore.Html

@ContentMarkup

@code {
  private RenderFragment? ContentMarkup { get; set; }

  protected override async Task OnInitializedAsync() {
    ContentMarkup = await DisplayAsync(Model.Content);
  }
}
