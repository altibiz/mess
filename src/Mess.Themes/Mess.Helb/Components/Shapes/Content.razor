@using Microsoft.AspNetCore.Html

@Body

@code {
  private RenderFragment? Body { get; set; }

  protected override async Task OnInitializedAsync() {
    Body = await DisplayAsync(Model.Content);
  }
}
